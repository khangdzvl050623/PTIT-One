// Barrel cho asset tĩnh. Component KHÔNG import thẳng file trong `assets/`,
// chỉ import qua barrel này (xem README) để đổi đường dẫn một chỗ duy nhất.
import ptitLogo from './uploads/logo/PTIT_LOGO.png'

export { ptitLogo }
